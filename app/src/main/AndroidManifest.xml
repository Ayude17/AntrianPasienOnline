<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simpus.antrianpasienonline">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CekHistoriAntrianActivity"></activity>
        <activity android:name=".DetailKartuAntrianActivity" />
        <activity android:name=".KonfirmasiActivity" />
        <activity android:name=".DaftarAntriActivity" />
        <activity android:name=".PilihDokterActivity" />
        <activity android:name=".PilihPoliActivity" />
        <activity android:name=".PilihTanggalActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DaftarActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashScreenActivity">

            <!-- android:theme="@style/AppThemeNoactionbar"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>